---
import { ArrowRightIcon } from '@heroicons/react/outline';
import TechIcon from './TechIcon.astro';
import { GitHubIcon } from '../icons/GitHubIcon.tsx';
const { kit } = Astro.props;
---

<div
  class="bg dark:dark-bg border border-gray-400 dark:border-gray-800 rounded-lg group hover:bg-blue-500 hover:dark:bg-gray-800 transition-colors overflow-hidden"
>
  <div class="px-4 py-6 space-y-4">
    {kit.technologies.map(({ name, Icon, tags }) => (
      <div class="flex items-center space-x-4">
        { Icon && <TechIcon {Icon} />}
        <div>
          <div class="text-xl t-dark dark:dark-t font-medium group-hover:text-white">{name}</div>
          <div class="t-light dark:dark-t-light group-hover:text-gray-100">{tags.join(', ')}</div>
        </div>
      </div>
    ))}
  </div>
  <div
    class="px-4 py-2.5 flex items-center justify-between border-t border-gray-200 dark:border-gray-800 dark:bg-gray-800 group-hover:bg-brand-600 group-hover:border-brand-600"
  >
    <a
      href={`/kit/${kit.name}`}
      class="inline-flex items-center justify-center t-light dark:dark-t group-hover:text-white font-medium text-lg"
    >
      See Kit Details <ArrowRightIcon className="inline w-4 h-4 ml-2" />
    </a>
    <a
      href={`https://github.com/thisdot/starter.dev/tree/main/starters/${kit.name}`}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center justify-center py-2.5 px-4 rounded-lg border border-gray-400 dark:border-gray-600 group-hover:border-white group-hover:bg-brand-500"
      ><GitHubIcon
        size={22}
        className="t-light dark:text-gray-100 group-hover:text-white"
      /></a
    >
  </div>
</div>
